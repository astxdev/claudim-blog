DATABASE_URL=postgresql://claudim:claudim@localhost:5433/claudim_blog
PAYLOAD_SECRET=troque-por-uma-string-aleatoria-longa

# De onde o frontend busca conteúdo (Payload REST API). Em dev, aponta pra
# si mesmo (este app roda o CMS embutido). Em produção na Vercel, aponta
# pro Payload que roda separado na VPS.
PAYLOAD_CMS_URL=http://localhost:3000/api
